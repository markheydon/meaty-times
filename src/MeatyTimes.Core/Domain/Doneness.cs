namespace MeatyTimes.Core.Domain;

/// <summary>
/// Desired doneness for meats where it affects cooking time (beef and lamb).
/// </summary>
public enum Doneness
{
    Rare,
    Medium,
    WellDone,
}
