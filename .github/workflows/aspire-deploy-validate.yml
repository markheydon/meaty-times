name: Validate Aspire deployment model

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]

permissions:
  contents: read

concurrency:
  group: aspire-deploy-validate-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  validate:
    name: Aspire deploy list-steps (Production)
    runs-on: ubuntu-latest
    timeout-minutes: 20
    env:
      DOTNET_CLI_TELEMETRY_OPTOUT: 1
      DOTNET_SKIP_FIRST_TIME_EXPERIENCE: 1
      DOTNET_NOLOGO: true

    steps:
      - name: Checkout repository
        uses: actions/checkout@v7

      - name: Set up .NET SDK from global.json
        uses: actions/setup-dotnet@v6
        with:
          global-json-file: global.json

      - name: Install Aspire CLI
        run: |
          curl -sSL https://aspire.dev/install.sh | bash -s -- --version 13.5.3
          echo "$HOME/.aspire/bin" >> "$GITHUB_PATH"

      - name: Restore and build solution
        run: |
          dotnet restore MeatyTimes.slnx
          dotnet build MeatyTimes.slnx --no-restore --configuration Release

      - name: Validate Aspire deployment steps
        run: aspire deploy --list-steps --apphost src/MeatyTimes.AppHost/MeatyTimes.AppHost.csproj --environment Production --non-interactive
